---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install hardening packages
  ansible.builtin.apt:
    name: "{{ os_hardening_packages }}"
    state: present
